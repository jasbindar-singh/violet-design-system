import TextInput from '.';

const meta = {
  component: TextInput,
};

export default meta;

export const EmailInput = {
  args: {
    type: 'email',
    label: 'Email',
    placeholder: 'Type your email...',
  },
};
