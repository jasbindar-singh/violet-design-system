import Button from '.';

const meta = {
  component: Button,
};

export default meta;

export const PrimaryButton = {
  args: {
    size: 'small',
    variant: 'primary',
    children: 'Button',
  },
};
